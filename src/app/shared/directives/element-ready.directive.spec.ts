import { ElementReadyDirective } from './element-ready.directive';

describe('ElementReadyDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementReadyDirective();
    expect(directive).toBeTruthy();
  });
});
