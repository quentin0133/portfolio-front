import { TypedTemplateDirective } from './typed-template.directive';

describe('TypedTemplateDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TypedTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
